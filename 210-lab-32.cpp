#include <iostream>
#include "Car.h"
#include <deque>
#include <random>
#include <ctime>

using namespace std;

int main() {
    deque<Car> booth(2);

    Car one;
    Car two;
    booth.push_back(one);
    booth.push_back(two);

    while(!(booth.empty())) {
        Car add
    }

    return 0;
}